<script>
    export let title;
</script>
<h3>{title}</h3>

<style>
    h3 {
        font-size: calc(var(--font-scale) * var(--font-scale) * var(--font-scale) * var(--font-scale) * var(--font-size));
    }
</style>